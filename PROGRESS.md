# 云迹科技 · 项目断点

## 当前状态

MVP 功能、依赖安装、正式构建和本地运行均已完成。

- 本机交付：`D:\云迹科技`
- 访问：`http://127.0.0.1:5173/`
- 启动/停止：双击 `启动云迹科技.bat` / `停止云迹科技.bat`
- 演示账号：`demo@yunji.tech` / `yunji2026`
- 详细运行说明和技术路线见 `README.md`。

## 已完成

- [x] React + Vite + TypeScript + Ant Design + ECharts
- [x] 登录、概览、客户、订单、库存、收支、统计、AI 助手八个页面
- [x] 四模块新增、编辑、删除、搜索、筛选与 CSV 导出
- [x] localStorage 持久化、JSON 备份恢复、异常数据保护
- [x] 回款自动同步收入、关联客户删除保护、SKU 唯一校验
- [x] 日报、周报、自然语言查数、未回款、库存预警、经营总结
- [x] 真实 LLM 服务端代理接口预留
- [x] 桌面与移动端布局，逐页和控制台检查
- [x] D 盘独立依赖、正式构建、一键启动/停止
- [x] README、技术路线与断点记录

## 已执行验证

| 验证 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| Vite 正式构建（开发目录及 D 盘） | 通过 |
| 业务计算与边界 | 7/7 通过 |
| 完整浏览器交互 | 16/16 通过 |
| 备份与恢复 | 5/5 通过 |
| 浏览器控制台 | 0 错误、0 警告 |
| 390px 手机工作台 | 7 页面无整页横向溢出，导航可操作 |

ECharts、Ant Design 分包超过 Vite 默认 500 kB 阈值，产生体积建议；构建成功。未来可按页面加载并进一步拆分组件库。

## 业务决定

1. 收入 = 订单已收款 + 手动收入；回款由账本函数派生，避免重复存储。
2. 回款按订单日期统计，净利润为收入减支出的演示口径。
3. 库存独立维护，订单不自动扣减；界面和 README 均已说明。
4. AI 基于当前数据运行本地规则，不支持的历史区间会明确提示。
5. 数据属于当前浏览器及地址，多标签并发编辑暂不合并冲突。

## 中断后继续

1. 阅读本文件、README 和 `git status`，不要重新初始化或覆盖项目。
2. 缺少依赖时运行 `npm install` 或 `pnpm install --frozen-lockfile`。
3. 运行 `npm run dev`；已有服务时复用上述地址。
4. 修改后运行对应测试，再执行 `npm run build`。
5. GitHub 目标：`https://github.com/pusok666/-.git`，分支 `main`。如提交后网络中断，检查 `git status`、`git remote -v` 后继续 `git push -u origin main`，不强制覆盖远程历史。

本机详细过程保存在未纳入 Git 的 `LOCAL_CHECKPOINT.md`，截图和测试结果在未纳入 Git 的 `test-results/`。
