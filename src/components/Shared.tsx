import { ArrowDownOutlined, ArrowUpOutlined, InfoCircleOutlined } from '@ant-design/icons';
import { Tooltip } from 'antd';
import type { ReactNode } from 'react';
export function PageHeading({eyebrow,title,description,extra}:{eyebrow?:string;title:string;description:string;extra?:ReactNode}) {return <div className="page-heading"><div>{eyebrow&&<div className="eyebrow">{eyebrow}</div>}<h1>{title}</h1><p>{description}</p></div><div className="heading-actions">{extra}</div></div>;}
export function MetricCard({title,value,icon,foot,tone='blue',hint}:{title:string;value:string|number;icon:ReactNode;foot:string;tone?:string;hint?:string}) {return <div className={`metric-card ${tone}`}><div className="metric-top"><span>{title} {hint&&<Tooltip title={hint}><InfoCircleOutlined/></Tooltip>}</span><span className="metric-icon">{icon}</span></div><div className="metric-value">{value}</div><div className="metric-bottom">{foot}</div><svg className="sparkline" viewBox="0 0 110 30" aria-hidden="true"><path d="M0 27 L12 23 L24 25 L36 17 L48 20 L60 10 L72 14 L84 5 L96 8 L110 1"/></svg></div>;}
export function Delta({value}:{value:number}) {return <span className={value>=0?'positive':'negative'}>{value>=0?<ArrowUpOutlined/>:<ArrowDownOutlined/>} {Math.abs(value).toFixed(1)}%</span>;}
export function Panel({title,subtitle,extra,children,className=''}:{title:string;subtitle?:string;extra?:ReactNode;children:ReactNode;className?:string}) {return <section className={`panel ${className}`}><div className="panel-head"><div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div>{extra}</div>{children}</section>;}
